Example application for React Formula.

It is linked to the react-formula package in the parent directory for development purposes.

You can run `npm install` and then `npm start` to run the example app.
